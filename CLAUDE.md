# シンプルシフト表（飲食店版）プロジェクト指示書

## 1. プロジェクト概要

- アプリ名：シンプルシフト表（飲食店版）
- 対象：小規模な飲食店の店長・経営者
- 主な利用端末：スマートフォン、iPad、パソコン
- 開発環境：Windows、VS Code、Live Server
- 公開方法：GitHub Pages
- 主なファイル：
  - index.html
  - style.css
  - script.js
- 基本方針：
  - 初めてシフトアプリを使う人でも迷わない
  - シンプルで直感的に操作できる
  - 各画面の目的を一つに絞る
  - 説明文を増やすより、配置や導線で自然に次の操作へ進める

## 2. 開発者への説明方法

利用者はプログラミング初心者です。説明するときは次を守ってください。

- 専門用語だけで説明しない
- 何をする作業なのかを先に説明する
- 操作手順を番号付きで示す
- VS Codeで編集する場合は、対象ファイル名を明示する
- 削除・置換する範囲を具体的に示す
- 「このあたり」ではなく、検索する文字列や開始位置・終了位置を示す
- 一度に多くの変更を詰め込まない
- コード変更後は、必ず動作確認方法も説明する
- 変更前に既存機能への影響を確認する
- 不明点があっても、既存コードを確認してから判断する
- 勝手に仕様を追加・変更しない
## 3. ファイル構成

- `index.html`：画面のHTML構造(シフト表の見た目・入力フォームなど)
- `style.css`：見た目のスタイル(色、レイアウト、フォントなど)
- `script.js`：動作の仕組み(シフトの追加・編集・削除などの処理)

## 4. よく使うコマンド

- 開発中の確認：VS Codeの拡張機能「Live Server」を使い、`index.html`を右クリック →「Open with Live Server」でブラウザ表示を確認する
- 公開：GitHubにプッシュ後、GitHub Pagesの設定から反映(反映まで数分かかる場合あり)

## 5. コーディングスタイル

- (インデントは半角スペース2つ、変数名は英語で分かりやすく、など、こだわりがあれば記載)

## 3. 最重要のUI・UX方針

- 初めて使う人が、説明を読まなくても自然に使い始められる画面にする
- 機能を説明するときは、仕組みより先に「利用者にとって何が便利になるか」を示す
- 初回設定では、シフト作成よりスタッフ登録を優先する
- スタッフを1人登録しただけで、シフト一覧を最優先表示に切り替えない
- 利用者が全スタッフの登録を終えてから、自然にシフト作成へ進めるようにする
- 不要な設定項目や確認画面を増やさない
- スマートフォンでも文字が小さくなりすぎないよう、自動的に最適化する
- 利用者が文字サイズを手動設定する機能は設けない

## 4. 初回画面とスタッフ登録

### スタッフが0人の場合

- 画面上部に「現在スタッフ登録 0人」と表示する
- 「スタッフを登録する」ボタンを目立たせる
- 最初に行う操作がスタッフ登録だと自然に分かる配置にする
- 新規シフト作成を最優先に表示しない

### スタッフ登録後

- 登録人数を「現在スタッフ登録 ○人」と表示する
- 1人登録後も、続けてスタッフを登録しやすい導線を維持する
- 利用者が登録完了と判断した後、自分で新規シフト作成へ進めるようにする

## 5. スタッフ管理画面

- カード形式ではなく、シンプルなリスト表示にする
- スタッフは登録順に表示する
- 各スタッフの行に「編集」「削除」ボタンを直接表示する
- 「︙」メニューの中に編集・削除を隠さない
- スタッフ一覧にはランチ・ディナーなどの種類を表示しない
- 「＋スタッフ追加」ボタンはスタッフ一覧の下に配置する

## 6. スタッフの種類

- 種類の例：ランチ、ディナー
- 1人のスタッフに複数の種類を設定できる
- 任意項目の初期チェックは入れない
- 種類の説明文は次を使用する

「複数のシフトを作成する店舗向け
スタッフに『ランチ』『ディナー』などの種類を設定しておくと、シフト作成時に該当するスタッフが自動で表示され、毎回スタッフを選び直す手間がありません。」

## 7. シフト作成

- ランチ用とディナー用のシフトを作成できる
- 基本入力項目は「シフト名」「シフト期間」「作成」
- 必要以上に入力項目を増やさない
- シフト作成時に設定された種類に応じてスタッフを自動表示する
- シフト画面からスタッフの追加・削除ができる
- 過去のシフトをコピーする「前回シフトコピー」を用意する
- 前日または翌日コピーは1クリックで行えるようにする
- コピー時に既存データがある場合は上書き確認を表示する
- 未入力データはコピーしない
- 1人分だけをコピーする機能は不要

## 8. 勤務区分

初期の勤務区分は次の通りです。

- ○（10:30〜）
- 休
- 17:00
- 17:30
- 17:45
- 18:00
- 18:15
- 18:30

表示ルール：

- 「○」は黒色、太字、大きめに表示する
- 店舗休業日は「休」と表示する
- 従業員個人の休みは空欄とする
- 人数カウントは表示しない

## 9. シフト表の表示

- 行には交互に背景色を付けて見やすくする
- 祝日は赤色で表示する
- ヘッダーは必要以上に大きくしない
- スマートフォン縦向きでは約4日分を見やすくする
- スマートフォン横向きでは約2週間分を確認しやすくする
- 横スクロールを使っても操作しやすくする
- 端末ごとに文字とレイアウトを自動調整する

## 10. 保存と共有

- PDF保存に対応する
- PDFはA4の1ページに収める
- LINE共有ではシフト表全体を1枚の画像として共有する
- LINE送信済みの場合だけ、送信済み表示を緑色にする
- バックアップと復元機能を維持する
- 既存の保存データとの互換性を壊さない

## 11. シフト一覧

- 現在使用中と次回使用予定のシフトを中心に表示する
- 過去のシフトは別の場所に整理して格納できる構成を検討する
- 一覧を不要な情報で複雑にしない
- シフトごとの操作は「︙」メニューにまとめる
- メニュー候補：
  - シフト名変更
  - LINE共有
  - PDF保存
  - 前回シフトコピー
  - 削除

## 12. 設定画面

設定画面はシンプルにし、次の分類を基本とする。

### データ管理

- バックアップ
- 復元
- スタッフデータ移行
- シフトデータ移行

### サポート

- 使い方
- お問い合わせ
- アプリを評価する

### アカウント

- 利用プラン
- バージョン

「引継ぎ」より「移行」という表現を優先する。

## 13. お知らせ機能

次のお知らせテンプレートを扱えるようにする。

- 臨時休業
- お盆
- 年末年始
- 営業時間変更
- 貸切
- ランチ休業
- ディナー休業

お知らせはLINE共有とPDF保存に対応する。

## 14. 多言語対応

将来的に次の言語へ対応する。

- 日本語
- 英語
- 中国語簡体字
- 韓国語

文字列をJavaScriptやHTMLの複数箇所へ直接重複して書かず、将来翻訳しやすい構成を優先する。

## 15. 課金方針

- 初月無料
- 月額課金を想定
- 先着利用者向け価格と通常価格を分ける可能性がある
- 課金機能は、利用者がアプリの便利さを理解してから案内する

## 16. 変更作業のルール

コードを変更するときは、次の順番で作業してください。

1. 現在のindex.html、style.css、script.jsを確認する
2. 関係する処理を特定する
3. 既存機能への影響を説明する
4. 必要最小限の変更を行う
5. HTML、CSS、JavaScriptの連携を確認する
6. ブラウザのコンソールエラーを確認する
7. 保存、再読み込み、主要操作を動作確認する
8. 変更内容と確認結果を分かりやすく報告する

## 17. 修正後に必ず確認する項目

- 初回画面が正しく表示される
- スタッフを追加できる
- スタッフを編集・削除できる
- 登録人数が正しく表示される
- シフトを新規作成できる
- シフト入力が保存される
- ページ再読み込み後もデータが残る
- 前日コピー・前回コピーが正しく動く
- LINE共有用画像が正しく作成される
- PDFがA4の1ページに収まる
- バックアップと復元が動く
- スマートフォン表示が崩れていない
- ブラウザの開発者ツールにエラーが出ていない

## 18. 禁止事項

- 利用者の許可なく機能を追加しない
- 決定済みの文言を勝手に変更しない
- 既存データを消す可能性がある変更を、説明なしで行わない
- index.htmlだけを変更して、必要なstyle.cssやscript.jsの確認を省略しない
- 動作確認をせずに「完了」と報告しない
- 専門用語だけで説明しない
- 大規模な全面書き換えを優先しない
- 利用者が依頼していないデザインや説明文を追加しない

## 19. Claude Codeへの報告形式

作業後は次の形式で報告してください。

### 変更したファイル

- ファイル名
- 変更した場所

### 変更内容

- 何を変更したか
- なぜ変更したか

### 動作確認

- 確認した操作
- 正常だった結果
- 未確認の項目

### 利用者が次に行うこと

初心者でも分かるように、番号付きで具体的に説明してください。
